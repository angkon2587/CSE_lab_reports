#include <stdio.h>

int main() {
    char currentLetter = 'a';

    while (currentLetter <= 'z') {
        printf("%c ", currentLetter);
        currentLetter++;
    }

    

    return 0;
}
